spring:
  datasource:
    url: jdbc:h2:file:./dbfile/db
#    文件数据 ##jdbc:h2:mem:db #内存数据
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true